# 2026-09-08 工作目录清理记录

## 清理范围

将 49 个早期产物压缩归档，逐文件校验 ZIP 内解压内容的 SHA-256 与原文件一致后，移除原工作目录中的散落副本。未直接丢弃唯一历史资料。

- `models/checkpoints/`：10 个旧中间检查点。
- 根目录 `logs/`：早期训练、随机评估、固定评估与 TensorBoard 日志；不涉及各正式运行内部的 logs。
- `results/diagnostics/`、`results/evaluations/`：早期设计修复诊断和评估。
- `results/report_assets/`、`results/trajectory_sheets/`：旧汇报图与旧轨迹图。
- 根 results 下的旧 episode_results、metrics_by_scenario_type、summary_metrics、training_reward_curve 及《USV_RL_汇报素材包.md》。

归档前共 6,043,632 字节，ZIP 为 4,249,781 字节。清理主要减少目录混杂，不是大规模磁盘释放。

归档：[legacy-pre-round0-20260908.zip](../archives/legacy-pre-round0-20260908.zip)。逐文件原路径、大小、哈希和 ZIP 哈希见 [归档清单](../archives/legacy-pre-round0-20260908.manifest.json)。旧文档中对应路径是当时的历史位置，现在可在该 ZIP 内按相同相对路径查找，不修改历史验收记录冒充原文件从未移动。

直接删除源码目录下可重新生成的 24 个 Python 字节码缓存文件，共 311,183 字节；不清理虚拟环境、Git 元数据、正式运行或其源码快照内部的文件。

## 保留及原因

- `runs/round0-*`、`runs/round1-*`、`runs/round2-*`：论文基线、消融、best/final、检查点、日志、源码快照和训练曲线，属于复现材料。
- `results/round1/`、`results/round2/`：封存结果及哈希清单，不动原始结果。
- `runs/verification/`：第零轮验收引用的续训冒烟证据，不是随意生成的临时垃圾。
- 根目录 `models/best/` 和 `models/final_model.zip`：现有评估默认路径仍可能回退到这里，保留以免清理改变默认行为；不因此认证其历史来源。正式实验应显式传入 run-dir。
- legacy/A/B 兼容代码、场景配置、生成脚本和测试：用于历史模型解码、消融和回归；未发现可在不影响这些功能的情况下直接删除的独立旧源码。
- `.venv/`、`.git/`、`.agents/`：依赖和项目元数据，保留。

未修改训练、环境、奖励、网络或封存评估脚本；没有重新训练，没有切换默认模型。

## 查阅与恢复

建议先解压到独立目录，仅取需要的文件，不直接覆盖当前项目：

```powershell
Expand-Archive -LiteralPath archives\legacy-pre-round0-20260908.zip -DestinationPath archives\legacy-review-20260908
```

如确需恢复旧入口，先对照归档清单检查哈希、确认目标位置没有新文件，再按原相对路径恢复。不要把归档内旧成绩当作本轮论文结果。

清理后环境、实验流程、第一轮、第二轮检查全部通过；3,600 回合轨迹审计通过，9 个封存模型和第零/第一轮 299 个封存产物未变，第二轮产物清单哈希全部通过，默认模型入口仍存在。最新进度见 [PROGRESS.md](PROGRESS.md)。
